<div class="row">
  <div class="col-auto offset-2 mt-5">
    <h2>所属グループ一覧</h2>
  </div>
</div>
<div class="row mt-5">
  <div class="col-8 mx-auto">

    <table class="table table-bordered border-dark">
      <thead>
        <tr>
          <th class="col-4">グループ名</th>
          <th class="col-5">グループ紹介</th>
          <th class="col-3"></th>
        </tr>
      </thead>
      <tbody>
        <% current_end_user.groups.each do |group| %>
          <tr>
            <td>
              <%= attachment_image_tag group, :image, size: "30x20", format: 'jpeg' %>  <%= group.name %>
            </td>
            <td>
              <%= group.introduction %>
            </td>
            <td>
              <div class="row">
                <div class="col-auto mx-auto">
                  <%= link_to "詳細", group_path(group), class: "btn btn-primary" %>
                </div>
              </div>
            </td>
          </tr>
        <%end%>
      </tbody>
    </table>
  </div>
</div>

current_user.groups.each do |group|